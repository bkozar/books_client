Before running the app, make sure that the server app is running.

Before running any app, install all the dependencies:
npm install

-----------------

To run the server app, execute command:
npm run start

To run the client app, execute command: 
npm run serve


